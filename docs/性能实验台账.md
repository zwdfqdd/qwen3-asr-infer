# 性能优化实验台账

本文件是 `dev` 分支从 v2.0.0 演进到 v3.0.0 的有效证据索引。策略定义、测试矩阵和准入规则见
[性能调优](性能调优.md)。未经目标 Linux/CUDA 实测的内容不得标记为“采纳”。

## 管理规则

- 实验 ID 使用 `PERF-<领域>-<三位序号>`，创建后不得复用；历史导入使用 `HIST-` 前缀。
- 状态只允许：`候选`、`就绪`、`测试中`、`采纳`、`拒绝`、`阻塞`。
- 一次只改变一个主要变量；每组至少预热 3 条并执行 3 轮，以中位数决策并保留最差有效轮。
- 保留可复现的最终验证、有效拒绝结论和必要根因摘要；删除被覆盖报告、临时诊断轮、重复抄录、
  异条件误对照及不能支持决策的数据。
- 原始音频、模型、完整业务日志和敏感正文不进入 Git；只提交去敏摘要、SHA256 和决策。
- 只有“采纳”且构成重要里程碑的提交创建带注释 tag。

## 实验索引

| ID | 状态 | 变量/目标 | 结论或下一步 |
|---|---|---|---|
| `HIST-A10-001` | 采纳 | 90 秒下比较 `LONG_CHUNKS_IN_FLIGHT=32/64/96/128` | 历史 A10 结果中 96 吞吐最高；须在当前默认 Aligner 下复测 |
| `HIST-A10-002` | 采纳 | 10 秒下比较客户端并发 96/128 | 128 吞吐约增 3.1% 但 P95 约增 29%；当前以 96 为起点 |
| `PERF-BASE-001` | 测试中 | 建立 v2.1.0 标准基线矩阵 | 10 秒默认时间戳基线已完成；下一步做同接口 ASR-only 对照 |
| `PERF-OBS-001` | 就绪 | 机器可读报告和环境/指标快照 | 实现已验证，采集开销仍待独立对照 |
| `PERF-OBS-002` | 就绪 | 请求阶段与排队耗时 | 目标 GPU 六轮计时覆盖率 100%，采集开销仍待独立对照 |
| `PERF-NET-001` | 采纳 | 消除网关到 vLLM 的失效连接复用 | v2.1.0；六轮 12000/12000，通过稳定性和性能保护门禁 |
| `PERF-LOAD-001` | 候选 | 混合时长及分桶 P95/P99 | 数据 manifest 完成后实施 |

## 实验记录模板

```markdown
## PERF-<领域>-<序号>：<标题>
- 状态、日期、代码提交/tag：
- 假设、唯一变量、基线与回滚值：
- 硬件、驱动/CUDA、依赖、模型 revision：
- 数据 manifest/SHA256、启动与压测命令：
- 三轮结果及中位数/最差有效轮：
- 资源、质量和错误契约：
- 相对基线变化、门禁结论和后续动作：
```
## PERF-OBS-001/002：性能证据与阶段计时

- 状态：就绪；日期：2026-07-27；实现提交包含于 `v2.1.0`。
- 压测器原子写入严格 JSON，记录 Git/运行环境、输入 SHA256、参数、错误分类、吞吐/延迟、资源、
  vLLM 指标前后快照及最多 20 个失败请求摘要；不保存音频内容、热词值或 `article_url` 值。
- 网关日志与 `Server-Timing` 共用 14 项阶段：请求解析、Base64、切片、分片本地/全局排队、
  后端 HTTP、ASR 墙钟、Aligner 排队/推理、后处理、序列化和总耗时。
- 无 GPU 契约冒烟通过；`PERF-NET-001` 六轮共 12000 个成功响应，阶段覆盖率均为 100%。
- 门禁：功能实现 GO；未执行关闭观测的同条件对照，因此采集开销尚未标记为“采纳”。

## PERF-BASE-001：10 秒默认时间戳稳定基线

- 状态：测试中；日期：2026-07-28；代码/tag：`v2.1.0`。
- 条件：`/chinese_asr` 自动语种、10.000 秒、1 个物理分片、默认字/词及句级时间戳、并发 96、
  每轮 2000 请求、强制完整 `Server-Timing`。
- 可靠报告：仅保留 `PERF-NET-001-keepalive4-run-01/02/03.json` 和
  `PERF-NET-001-keepalive4-confirm-run-01/02/03.json`；六份报告共同构成本基线。

| 轮次 | 成功/总数 | QPS | audio_s/s | 平均延迟 ms | P50 ms | P95 ms | P99 ms |
|---|---:|---:|---:|---:|---:|---:|---:|
| run-01 | 2000/2000 | 11.09 | 110.90 | 8526.7 | 8799.1 | 8916.4 | 8932.7 |
| run-02 | 2000/2000 | 10.68 | 106.82 | 8830.0 | 9051.7 | 9409.4 | 9563.7 |
| run-03 | 2000/2000 | 10.82 | 108.21 | 8739.1 | 8862.9 | 9258.2 | 9404.4 |
| confirm-01 | 2000/2000 | 11.02 | 110.16 | 8579.8 | 8795.5 | 9145.2 | 9261.7 |
| confirm-02 | 2000/2000 | 10.99 | 109.89 | 8605.2 | 8794.0 | 9025.2 | 9306.7 |
| confirm-03 | 2000/2000 | 11.07 | 110.69 | 8543.6 | 8773.8 | 8956.1 | 8998.5 |
| 六轮中位数 | 12000/12000 | 11.005 | 110.025 | 8592.5 | 8797.3 | 9085.2 | 9284.2 |

- 阶段均值中位数：`asr` 452.32 ms、`aligner-queue` 8083.30 ms、`aligner` 89.03 ms、
  `total` 8582.09 ms；Aligner 排队约占总耗时 94.19%，是下一阶段的确定瓶颈。
- 资源：GPU 平均利用率中位数 97.55%，峰值 100%，显存峰值始终 19117 MiB；CPU 平均利用率
  中位数 12.8%。最差有效轮为 run-02：106.82 audio_s/s、P95 9409.4 ms、P99 9563.7 ms。
- 质量：六轮累计 12000/12000，文本长度稳定为 67 字符，`Server-Timing` 覆盖率 100%，无
  `MODEL_LOAD_FAILED`；该条件 GO。标准矩阵尚未完成，下一步是同一 `/chinese_asr` 路径关闭
  两个时间戳开关的 ASR-only 三轮对照。
## PERF-NET-001：后端空闲连接稳定性

- 状态：采纳；日期：2026-07-28；代码/tag：`v2.1.0`（`5f1ffe6`）。
- 根因：aiohttp 默认空闲连接保留 15 秒；默认时间戳请求受串行 Aligner 限制，池内连接可能先被
  vLLM 关闭，复用时触发毫秒级 `ServerDisconnectedError`。修复前稳定性摘要为 5/6000 失败；
  该摘要仅证明问题存在，不作为性能基线。
- 采纳值：`BACKEND_CONNECTION_LIMIT=100`、`BACKEND_KEEPALIVE_TIMEOUT=4`；使用显式
  `TCPConnector(limit=100, limit_per_host=100, keepalive_timeout=4)`，不自动重试 POST。
- 可观测性：`backend_chunk_failed` 保留底层异常类型、cause 深度及可用 errno；压测报告保留
  有界失败请求 ID，便于关联网关与 vLLM 日志。
- 最终证据：采用 PERF-BASE-001 所列六份可靠报告，累计 12000/12000、计时覆盖率 100%。相对
  修复前成功请求摘要，六轮中位 QPS -1.03%、audio_s/s -1.09%、平均延迟 +1.12%、P95 +2.54%、
  P99 +4.07%、服务端 `total` P99 +3.04%，均在 5% 保护线内。
- 决策：GO。更短 idle 在低 QPS 下会增加本机 TCP 建连，后续长音频和混合负载继续观察 CPU、
  连接错误与 P99。
- 回滚：设置 `BACKEND_KEEPALIVE_TIMEOUT=15` 可恢复旧行为；若再次出现连接失败，不得将结果
  作为正式基线。

## 数据保留结论

- 保留：上述六份 keepalive4 JSON、对应环境/输入摘要、`v2.1.0` 代码与本台账最终汇总。
- 删除：`PERF-BASE-001-short-run-01/02/03.json`、`PERF-NET-001-diagnostic-run-01.json` 等失败、
  被覆盖或仅用于定位的过程报告；删除未指定 `--api-mode chinese-asr` 的异条件 native 结果抄录。
- 当前仓库未发现 `performance-results` 文件；目标机报告需按以上清单清理。原始日志仅按既定
  轮转和保留策略管理，不因本次文档清理删除审计日志。
